# Generated by Cargo
# will have compiled files and executables
debug/
target/

# We ship binaries — keep Cargo.lock so reproducible builds (CI, AUR, RPM) work.
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# downloaded release assets / local test installs
/protondrive-linux/
/protondrive-linux-*.pkg.tar.zst
/protondrive-linux-*.deb
/protondrive-linux-*.rpm
/protondrive-linux-*.flatpak
/protondrive-linux-*.AppImage
integration
